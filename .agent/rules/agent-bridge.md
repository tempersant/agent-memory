# Agent Bridge (Antigravity rule)

Read `.agent/HANDOFF.md` and `.agent/state.json` at the start of every task.

Before finishing:

- Update `state.json` with actor `antigravity`
- Rewrite `HANDOFF.md`
- Append to `events.jsonl`
- Remind user to commit `.agent/`

Full protocol: `AGENTS.md` and `GEMINI.md`.
